#include <stdio.h>
int main(){
    char name[]= "Bande";
    int age = 64;
    char assigned_letter = 'Z';
    float height = 6.3;

    printf("%s is %d years old.He is %fft tall and live in street %c",name,age,height,assigned_letter);
}